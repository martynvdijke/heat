version: '3.8'

services:
  heat:
    image: ghcr.io/martynvdijke/heat:latest
    volumes:
      - heat-data:/app
      - heat-db:/db
    container_name: heat
    restart: unless-stopped

volumes:
  heat-data:
  heat-db:
