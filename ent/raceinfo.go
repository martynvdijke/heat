// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"heat/ent/raceinfo"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// RaceInfo is the model entity for the RaceInfo schema.
type RaceInfo struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Country holds the value of the "country" field.
	Country string `json:"country,omitempty"`
	// Track holds the value of the "track" field.
	Track string `json:"track,omitempty"`
	// TrackID holds the value of the "track_id" field.
	TrackID string `json:"track_id,omitempty"`
	// Laps holds the value of the "laps" field.
	Laps         int `json:"laps,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*RaceInfo) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case raceinfo.FieldID, raceinfo.FieldLaps:
			values[i] = new(sql.NullInt64)
		case raceinfo.FieldCountry, raceinfo.FieldTrack, raceinfo.FieldTrackID:
			values[i] = new(sql.NullString)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the RaceInfo fields.
func (_m *RaceInfo) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case raceinfo.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case raceinfo.FieldCountry:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field country", values[i])
			} else if value.Valid {
				_m.Country = value.String
			}
		case raceinfo.FieldTrack:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field track", values[i])
			} else if value.Valid {
				_m.Track = value.String
			}
		case raceinfo.FieldTrackID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field track_id", values[i])
			} else if value.Valid {
				_m.TrackID = value.String
			}
		case raceinfo.FieldLaps:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field laps", values[i])
			} else if value.Valid {
				_m.Laps = int(value.Int64)
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the RaceInfo.
// This includes values selected through modifiers, order, etc.
func (_m *RaceInfo) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// Update returns a builder for updating this RaceInfo.
// Note that you need to call RaceInfo.Unwrap() before calling this method if this RaceInfo
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *RaceInfo) Update() *RaceInfoUpdateOne {
	return NewRaceInfoClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the RaceInfo entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *RaceInfo) Unwrap() *RaceInfo {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: RaceInfo is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *RaceInfo) String() string {
	var builder strings.Builder
	builder.WriteString("RaceInfo(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("country=")
	builder.WriteString(_m.Country)
	builder.WriteString(", ")
	builder.WriteString("track=")
	builder.WriteString(_m.Track)
	builder.WriteString(", ")
	builder.WriteString("track_id=")
	builder.WriteString(_m.TrackID)
	builder.WriteString(", ")
	builder.WriteString("laps=")
	builder.WriteString(fmt.Sprintf("%v", _m.Laps))
	builder.WriteByte(')')
	return builder.String()
}

// RaceInfos is a parsable slice of RaceInfo.
type RaceInfos []*RaceInfo
