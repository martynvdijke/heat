// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"heat/ent/racer"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// Racer is the model entity for the Racer schema.
type Racer struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Name holds the value of the "name" field.
	Name string `json:"name,omitempty"`
	// ProfilePicture holds the value of the "profile_picture" field.
	ProfilePicture string `json:"profile_picture,omitempty"`
	// CarColor holds the value of the "car_color" field.
	CarColor string `json:"car_color,omitempty"`
	// CarName holds the value of the "car_name" field.
	CarName string `json:"car_name,omitempty"`
	// Points holds the value of the "points" field.
	Points int `json:"points,omitempty"`
	// Rank holds the value of the "rank" field.
	Rank int `json:"rank,omitempty"`
	// Position holds the value of the "position" field.
	Position int `json:"position,omitempty"`
	// TeamID holds the value of the "team_id" field.
	TeamID       int `json:"team_id,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Racer) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case racer.FieldID, racer.FieldPoints, racer.FieldRank, racer.FieldPosition, racer.FieldTeamID:
			values[i] = new(sql.NullInt64)
		case racer.FieldName, racer.FieldProfilePicture, racer.FieldCarColor, racer.FieldCarName:
			values[i] = new(sql.NullString)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Racer fields.
func (_m *Racer) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case racer.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case racer.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				_m.Name = value.String
			}
		case racer.FieldProfilePicture:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field profile_picture", values[i])
			} else if value.Valid {
				_m.ProfilePicture = value.String
			}
		case racer.FieldCarColor:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field car_color", values[i])
			} else if value.Valid {
				_m.CarColor = value.String
			}
		case racer.FieldCarName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field car_name", values[i])
			} else if value.Valid {
				_m.CarName = value.String
			}
		case racer.FieldPoints:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field points", values[i])
			} else if value.Valid {
				_m.Points = int(value.Int64)
			}
		case racer.FieldRank:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field rank", values[i])
			} else if value.Valid {
				_m.Rank = int(value.Int64)
			}
		case racer.FieldPosition:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field position", values[i])
			} else if value.Valid {
				_m.Position = int(value.Int64)
			}
		case racer.FieldTeamID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field team_id", values[i])
			} else if value.Valid {
				_m.TeamID = int(value.Int64)
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Racer.
// This includes values selected through modifiers, order, etc.
func (_m *Racer) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// Update returns a builder for updating this Racer.
// Note that you need to call Racer.Unwrap() before calling this method if this Racer
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *Racer) Update() *RacerUpdateOne {
	return NewRacerClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the Racer entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *Racer) Unwrap() *Racer {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: Racer is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *Racer) String() string {
	var builder strings.Builder
	builder.WriteString("Racer(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("name=")
	builder.WriteString(_m.Name)
	builder.WriteString(", ")
	builder.WriteString("profile_picture=")
	builder.WriteString(_m.ProfilePicture)
	builder.WriteString(", ")
	builder.WriteString("car_color=")
	builder.WriteString(_m.CarColor)
	builder.WriteString(", ")
	builder.WriteString("car_name=")
	builder.WriteString(_m.CarName)
	builder.WriteString(", ")
	builder.WriteString("points=")
	builder.WriteString(fmt.Sprintf("%v", _m.Points))
	builder.WriteString(", ")
	builder.WriteString("rank=")
	builder.WriteString(fmt.Sprintf("%v", _m.Rank))
	builder.WriteString(", ")
	builder.WriteString("position=")
	builder.WriteString(fmt.Sprintf("%v", _m.Position))
	builder.WriteString(", ")
	builder.WriteString("team_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.TeamID))
	builder.WriteByte(')')
	return builder.String()
}

// Racers is a parsable slice of Racer.
type Racers []*Racer
